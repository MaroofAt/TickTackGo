import 'package:flutter/material.dart';

class PointsStatistics extends StatelessWidget {
  const PointsStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: ,
    );
  }
}

